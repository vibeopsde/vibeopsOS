# vibeopsOS Global Agent Configuration

You are working in a vibeopsOS environment - a development-focused Ubuntu distribution with OpenCode AI coding agent pre-installed.

## Environment Rules

- Use `sudo` without password (user has sudo privileges)
- Always suggest testing before committing
- Prefer modern, secure patterns
- Document decisions in code comments

## Project Standards

- Write clean, readable code
- Use meaningful commit messages
- Include CI/CD where applicable
- Test before merge

## Available Project Templates

Navigate to `~/vibeops/projects/` to find project templates:
- `web/` - Web development (Node.js, TypeScript)
- `python/` - Python projects (Poetry, pytest)
- `general/` - General purpose

## First Steps

1. Check `~/vibeops/` for project structure
2. Look at `~/vibeops/projects/` for templates
3. Start with `opencode .` in your project folder

Good luck, and build something great!